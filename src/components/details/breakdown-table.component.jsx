import React from "react";
import Table from "../common/table.jsx";

const BreakdownTable = ({ data: payments, name }) => {
  return (
    <div>
      {/* <h1>{name}</h1>
      <Table data={payments} /> */}
      <h1>Breakdown Table</h1>
    </div>
  );
};

export default BreakdownTable;
